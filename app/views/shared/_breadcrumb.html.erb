<div class="topbar">
  <div class="container">
    <div class="vertical-center">
      <%= link_to 'solidarity', root_path %> > <% if local_assigns.has_key? :category %> <%= link_to @category.name, @category %> > <% end %><%= current_page %>
    </div>
  </div>
</div>
